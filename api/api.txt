create a page to doc (link) all the APIs (12)
	Command, ComprOps, ConfigDef, ConfigSetType, ConnAccount, Connection,
	CryptModuleSpecs, ICommand, ImapAuth, Menu, MxOps, StoreOps
	Path (WIP)
plus all the typedef'd function callbacks (19)
	addr_predicate_t, bcache_list_t, config_init_t, eat_arg_t, encoder_t,
	format_t, handler_t, hash_cmp_key_t, hash_gen_hash_t,
	hash_hdata_free_t, hcache_namer_t, list_free_t, log_dispatcher_t,
	mutt_file_map_t, observer_t, parser_callback_t, pop_fetch_t,
	sig_handler_t, sort_t

check "const" ∀ API function params

API contract guarantees callback functions won't be passed NULL?

alias api
history API
progress API
colours API
Search/Limit API
browser api

APIs
	address
	alias (contacts, manage own file)
	completion
	history
	menu
	progress
	questions
	regex
	windows

