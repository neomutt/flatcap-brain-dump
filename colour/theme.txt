Theme Colours

colours
	multiple domains per object (simple, palette, rgb)
	need linking together

parse deep colours, then *cull* on gui_init()

color object *link* object2
use same linkage mechanism for multiple colour domains

arrange: object -> rgb -> palette -> simple
where are the attrs stored? *separately*
if rgb isn't valid, nor are its attrs -- culled

if we *cull* them, then :color isn't useful
flag unusable colours instead
:color -> # not supported (instead of swatch)

~/.neomutt-theme
~/.config/neomutt/theme.rc

theme-light.rc
theme-dark.rc
	or some config way to select the right file
$background = [unknown|light|dark]
	source theme-$background.rc
if $background is manually set, don't query the terminal

source! -- no fail
and/or
source *dir*  ∀ *.rc within (alpha sorted)


if theme contains
	color x rgb     rgb
	color x palette palette
	color x simple  simple
and all three domains are supported
what happens when the user does?
	color x palette palette
result?
	overwrite just the palette?
	overwrite all?
		because it's interactive?
does DT_STARTUP allow stacking of colours?
then runtime if colour domain is allowed, overwrite all
what if the user sources a theme again?

hundreds of themes that can be easily converted
vim theme looks easy
	https://github.com/mbadolato/iTerm2-Color-Schemes

turn all neomutt themes into SVG
	https://github.com/wader/ansisvg

limit `theme` command to `color` commands only
	flag `theme`-compatible commands
		might need some config options => `set`, etc
		`set my_var` would be very useful
	default to quiet, but log everything
		except syntax errors
		except forbidden commands

:theme [flags] {name}
	-n	next theme     (wrap around?)
	-p      previous theme (wrap around?)
	-d	default theme
no params => show theme name (and file location)
:theme NAME
	load named theme from a "standard" location
		message "Theme: NAME"
	$theme_dir
	/usr/share/neomutt/themes
	~/.config/neomutt/themes

need text "screenshots" covering all *colours*
	they need colour {placeholders}
	script to turn them into ANSI colours
	then covert them into svg
	push to dedicated theme-display repo/website

theme: check which colors aren't used -- fit them in
	which colors are diff from mutt?
	(every color should be used)
	trawl config/ for colour pattern ideas
		diff
		markdown
		urls
	search for mutt themes

https://github.com/catppuccin/neomutt
https://github.com/altercation/mutt-colors-solarized

themes repo
	need dns
	light
		palette/
		rgb/
	dark
		palette/
		rgb/
	scripts/
	website/	(RTD)
	screenshots
	README.md

need some light/dark elements that are shared

theme list need colour swatch for each

<screenshot> global function would be hard
	by the time the text is actually painted
	the colours have become merged.
	The caller does the merging, so paint gets a merged colour.
	there's no way to work out which MT_COLOR_* parts it's made from.
	To fix this, we'd have to keep a ColorArray
	Index line = [ NORMAL, INDEX_COLUMN, INDEX_ROW, INDEX, INDICATOR]
	then window_write(text, colour array)
	THEN, the low-level writer knows which bits of which colours remain
	can generate: <span class="indicator, index_row"> (bg,fg)
	helper functions for the ColorArray:
	push(NORMAL); push(INDEX_COLUMN); push(INDEX_ROW); STUFF; pop(2); pop(1);
	lots of work, but separates the index,etc, from the colour details
	ColorArray should cache the merged colours
	Merged Colours are cached lazily as their used.
	Important: this preserves the colours, even if they're not set!
	how do ANSI colours fit in?
		push(MT_COLOR_CUSTOM, AttrColor)
		caller _does_ need some awareness of colours, here
	what about RegexColors with multiple matches?
		for (RegexColors): if RegexMatch: push(Color)
		caller deals with matching
		library deals with colours
	ColorArray:
		enum ColorId id;		// Id, e.g. MT_COLOR_NORMAL
		struct AttrColor *ac;		// Looked up colour
	<screenshot> can list colours used
		can even list some colours that exist but weren't visible. NO
		the non-visible colours could be visible in *certain* themes


theme list, zip them (truecolor,palette) and make them available to download
	or make a release and add them to the release artifacts

themes: list and credit source
	explanation of how the site works

theme viewer?
	keep track of themes applied
	allow <prev> <next>
	<random> just adds to the list
	temp theme-name sidebar, with swatches
	fuzzy search, <up>, <down> live updates to rest of screen

theme viewer:
	Menu of: | Name | Swatch |
	sorted by name?
		favourites first?
		with separator
	add Favourites column
	or rating, or star count -- HOW?
		=> sorting
	split screen?
		menu at top
		sample panel below?
	live view?
		first line is current theme (or user's custom settings)
		menu of themes below
		changing the selection changes the theme *live*
		<top> -> back to user's settings
		<enter> -> select theme, quit
		<quit> -> restore user's theme, quit
		need way to save/restore user's entire colour setup
	probably better than <theme-next/prev> when there are lots of themes
	<random> function!

theme-hook?
	Setting a theme probably clears all settings first
	(only way to guarantee behaviour)
	When a theme is set, let the user customise it
	theme-hook NAME (specific)
	theme-hook .    (for all)
	folder-hook +work "<enter-command>theme xyz<enter>"
		auto-triggers theme-hook
	overkill?
		why not just source a file with "theme X; color *"?

