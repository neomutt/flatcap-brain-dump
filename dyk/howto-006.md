Howto 006 — Using display_filter and sed to tidy automated emails

Simple sed/awk/grep examples to remove boilerplate or trim long logs, how to wire them into folder-hook or mailbox-specific display_filter, and examples for JIRA-style emails. Include a testing strategy: copy a sample message to a test mailbox and iterate until the filter is safe.