DYK 018 — Use tag-prefix to operate on many messages

Tag a set of messages (;) and then use tag-prefix to run a macro or command on all tagged messages. Many builtin actions honour the tag-prefix so you can apply the same operation to multiple emails without scripting.