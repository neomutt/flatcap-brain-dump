DYK 017 — Test command expansion safely

Before running complex :push or :exec sequences, preview their results with :echo. Example:
:echo "push '<search>~i MSG'<enter>"
This prints the expanded string instead of executing it so you can confirm behavior.