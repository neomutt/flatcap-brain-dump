DYK 021 — Clean junk from incoming mail with display_filter

Use display_filter (or folder-specific folder-hook + display_filter) to postprocess messages for readability. Example to strip quoted trails:
set display_filter = "sed '/^On/,/^$/d'"
You can also use grep -v to hide autoview noise.