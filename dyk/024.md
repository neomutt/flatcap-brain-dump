DYK 024 — Use folder-hook to export the current folder to scripts

folder-hook can set environment variables you reference from external scripts. Example:
folder-hook . 'setenv MYCURFOLDER "^"'
Then a shell script called by a command can read $MYCURFOLDER to know which mailbox is active.