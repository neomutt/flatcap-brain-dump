DYK 028 — Quick notmuch setup tip

To use notmuch with neomutt set nm_default_url to your notmuch DB path. Example:
set nm_default_url = "notmuch:///home/user/.local/share/notmuch"
Then use vfolder-from-query or named virtual mailboxes to integrate searches into the sidebar.