DYK 036 — Use index-format-hook for relative dates

Use index-format-hook to display friendly date labels like “just now”, “yesterday” or “this week” depending on age. Example:
index-format-hook date "~d<1H" "just now"
This improves glancing at recent mail at a single glance.