DYK 032 — Disable keys silently with end-cond

Bind a key to end-cond or nop in a specific context to make it a no-op without noisy errors. Example:
:bind index p end-cond
This makes 'p' do nothing quietly when you're in the index.