DYK 042 — Use display_filter to decode or reformat content

display_filter can pipe message content through external tools (iconv, pandoc, sed) to make ugly emails readable. Example:
set display_filter = "iconv -f ISO-8859-1 -t UTF-8"
Useful when senders use legacy encodings or irregular formatting.