DYK 027 — Hide Autoview noise

Autoview can produce noisy headers like "[-- Autoview using ...]". Hide or filter them:
color attach_headers black black "\\[--\\ Autoview.*\\]$"
set display_filter = 'grep -v "\\[-- Autoview using"'
This keeps the pager less cluttered.