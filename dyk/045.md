DYK 045 — Use save and copy wisely for archiving vs forwarding

save copies the message into a mailbox, copy|append differences depend on backend; test with small messages to confirm behaviour. Use save hooks for automated archival and use copy when you want an editable forwarded message.