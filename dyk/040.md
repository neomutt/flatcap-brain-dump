DYK 040 — Use limit to focus an index quickly

Use :limit to narrow the index to a query and then :unlimit to restore. Example:
:limit ~t flatcap@example.com
This is a fast way to focus on a single sender or topic without changing mailboxes.