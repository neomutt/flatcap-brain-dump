DYK 016 — Avoid polluting history

Prefix interactive commands with a leading space to avoid them being saved in neomutt's command history. Example: " :source myprofile.rc" (note the space before the colon). Useful for temporary commands, testing, or hiding credentials entered interactively.