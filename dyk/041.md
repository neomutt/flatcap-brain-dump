DYK 041 — Tag-pattern to mark many messages

Use :tag-pattern with search expressions to bulk-tag messages programmatically. Example:
:tag-pattern ~f "noreply@" +
Combine with tag-prefix macros to batch-process the results.