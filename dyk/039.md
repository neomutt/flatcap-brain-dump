DYK 039 — Use sort and thread options to control view

Adjust sort and thread settings to see conversation order and priorities you care about. Examples:
set sort = "date"
set sort_aux = "reverse-last-date"
set strict_threading = yes
Try combinations to find what surfaces important messages.