release VERSION in *.man *.[15]
	script ∀ files

update doxygen docs

release branch
	no .travis.yml (no building)
	ChangeLog
	configure.ac
	README

update releases page
update en_GB to 100%

add vim repo to release notes "to check" list

check for co-author commits

always send release message to neo mailing lists
	using new version "User Agent:"

on release update po/*
	reduce number of fuzzy translations?

push to coverity, review changes

update vim syntax with new variables, etc
./check_sec.sh

update my alias list with all contributors

add: /cc @neomutt/downstream-packagers

link to travis/coverity

sync mutt/default every release
ugly, but keeps tree in check & gh status

tag website (for ref purposes)

strip (#mutt-trac) from upstream commits?

clang-format, then update-po

put rel commits in side-branch
	update ver, authors, trans, cocci, clang
	merge commit == release?

update mailmap

post comment to aur package

update mutt releases web page

Release commits:
	version number (configure.ac)
                doxygen config
	changelog
	mailmap/readme credits
	run cocci, etc
	clang-format
	docs
		check indent
		update docs dates / versions
		make validate
		make spellcheck
		make sortcheck
	translations
		merge
		update project date
	sync to mutt/default-clean

version
credits
mailmap
changelog
clang-format
whitespace
translations, dates and hi-scores
sync git

sync mutt/{stable,default} with clean

on dev release
        user-ml: coming soon email

add management repo to credit list
add neomutt-docs repo to credit list
consider other repos for rel notes?
perform NULL sync to mutt/default-clean

update website from docs (after version number)

split release notes into dev & stable
dev
        full details
        long and technical
        include issues
stable
        short
        features
        user-visible fixes

VERSION
	ChangeLog.md
	configure.ac
	doc/manual.xml.head
	doxygen/doxygen.conf
	po/mutt.pot

TRANSLATIONS
	update translations
	update en_GB.po

CREDIT
	update mailmap (and management copy)
	update README credits (and website copy)

SOURCE
	tidy whitespace
	clang-format

DOCS/WEB
	update guide
	update readme/credits
