struct Mailbox *mutt_index_menu(struct MuttWindow *dlg, struct Mailbox *m_init)
{
  // SET UP
  while (true)
  {
    // PRE-AMBLE
    switch (op)
    {
      case ABC:
        // ...
    }
    // POST-AMBLE
  }
  // CLEAN UP
}
